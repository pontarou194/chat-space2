.side-bar
  .header
    %h3.header__name
      = current_user.name
    %ul.header__lists
      %li.list
        = link_to new_group_path do
          = icon("fas", "star")
      %li.list
        = link_to edit_user_path(current_user) do
          = icon("fas", "star")
  .groups
    - current_user.groups.each do |group|
      .group
        = link_to group_messages_path(group) do
          .group__name
            = group.name
          .group__message
            投稿したメッセージの確認、これはただのコメント




-#  .groups
-#     - current_user.groups.each do |group|
-#       .group
-#         = link_to group_messages_path(group) do
-#           .group__name
-#             = group.name
-#           .group__message
-#             メッセージはまだありません。


//groups以下のこれを貼っていた
-# .group
-#       .group__name
-#         グループ名
-#       .group__message
-#         新着メッセージ
-#     .group
-#       .group__name
-#         グループ名
-#       .group__message
-#         新着メッセージ
-#     .group
-#       .group__name
-#         グループ名
-#       .group__message
-#         新着メッセージ