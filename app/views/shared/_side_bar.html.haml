.side-bar
  .header
    %h3.header__name
      = current_user.name
    %ul.header__lists
      %li.list
        = link_to new_group_path do
          %i.fas.fa-pen-square.fa-lg.my-white
      %li.list
        = link_to edit_user_path(current_user) do
          %i.fas.fa-user-edit.fa-lg.my-white
  .groups
    - current_user.groups.each do |group|
      .group
        = link_to group_messages_path(group) do
          .group__name
            = group.name
          .group__message
            = link_to group_messages_path(group) do




-#  .groups
-#     - current_user.groups.each do |group|
-#       .group
-#         = link_to group_messages_path(group) do
-#           .group__name
-#             = group.name
-#           .group__message
-#             メッセージはまだありません。


//groups以下のこれを貼っていた
-# .group
-#       .group__name
-#         グループ名
-#       .group__message
-#         新着メッセージ
-#     .group
-#       .group__name
-#         グループ名
-#       .group__message
-#         新着メッセージ
-#     .group
-#       .group__name
-#         グループ名
-#       .group__message
-#         新着メッセージ